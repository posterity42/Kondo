// auto generated by kmigrator

exports.up = async (knex) => {
    await knex.raw(`
    BEGIN;
--
-- Create constraint payment_unique_provider_reference_per_scope on model payment
--
CREATE UNIQUE INDEX "payment_unique_provider_reference_per_scope" ON "Payment" ("organization", "provider", "providerReference")
WHERE "deletedAt" IS NULL AND "providerReference" IS NOT NULL;
COMMIT;

    `)
}

exports.down = async (knex) => {
    await knex.raw(`
    BEGIN;
--
-- Create constraint payment_unique_provider_reference_per_scope on model payment
--
DROP INDEX IF EXISTS "payment_unique_provider_reference_per_scope";
COMMIT;

    `)
}
